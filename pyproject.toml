[project]
name = "master-thesis"
version = "0.1.0"
description = "Evaluate collaborative strategies for local language models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]==0.139.0",
    "lm-eval[api]==0.4.12",
    "matplotlib==3.11.1",
    "ollama==0.6.2",
]

[tool.uv]
add-bounds = "exact"

[tool.fastapi]
entrypoint = "main:app"

[tool.ruff.lint]
extend-select = ["I"]

[dependency-groups]
dev = [
    "mypy==2.1.0",
    "pre-commit==4.6.0",
    "pylint==4.0.6",
    "pysonar==1.6.0.4905",
    "ruff==0.15.20",
    "types-pyyaml==6.0.12.20260815",
]
